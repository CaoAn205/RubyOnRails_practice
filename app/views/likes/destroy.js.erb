// debugger
$(`#like-icon-post-<%= @post.id%>`).
  html('<%= link_to "Loved", post_likes_path(@post), method: :post, remote: true, class: "core-sprite love hide-text" %>');
$(`#like-text-post-<%= @post.id%>`).
  html('<%= j render "posts/like_text", {likes: @post.likes} %>');
console.log("delete like success");
// <%= link_to "Loved", post_likes_path(@post), method: :post, remote: true, class: "core-sprite love hide-text" %>
