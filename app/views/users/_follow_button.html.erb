<% if user.is_followed %>
  <%= link_to "Followed", follow_path(follow.id), method: :delete, class: "btn common-btn followed-user-btn", remote: true %>
<% else %>
  <%= link_to "Follow", user_follows_path(user.id), method: :post, class: "btn common-btn follow-user-btn", remote: true %>
<% end %>
